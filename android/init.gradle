gradle.startParameter.excludedTaskNames += 'generateLockfiles'

allprojects {
    tasks.whenTaskAdded { task ->
        if (task.name == 'generateLockfiles') {
            task.enabled = false
        }
    }
} 